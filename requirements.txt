# =============================================================================
# Core Dependencies (Cloud-Agnostic)
# =============================================================================
# Works on: Any platform - local, Docker, Kubernetes, serverless, PaaS
# =============================================================================

# Core Flask
flask>=3.0.0
flask-wtf>=1.2.0
flask-talisman>=1.1.0
flask-limiter>=3.5.0

# Configuration & Environment
python-dotenv>=1.0.0

# Production WSGI Server
gunicorn>=21.0.0

# Security & Validation
bleach>=6.1.0
email-validator>=2.1.0

# =============================================================================
# Optional: Cloud Provider SDKs (uncomment as needed)
# =============================================================================
# These are OPTIONAL - the app works without them
# Install only if deploying to specific cloud providers

# AWS (Lambda, ECS, Secrets Manager)
# boto3>=1.34.0

# Google Cloud (Cloud Run, Secret Manager)
# google-cloud-secret-manager>=2.18.0

# Azure (Container Apps, Key Vault)
# azure-identity>=1.15.0
# azure-keyvault-secrets>=4.7.0

# =============================================================================
# Optional: Observability (uncomment as needed)
# =============================================================================

# Error tracking
# sentry-sdk[flask]>=1.40.0

# Prometheus metrics
# prometheus-flask-exporter>=0.23.0

# OpenTelemetry (for distributed tracing)
# opentelemetry-api>=1.22.0
# opentelemetry-sdk>=1.22.0
# opentelemetry-instrumentation-flask>=0.43b0

